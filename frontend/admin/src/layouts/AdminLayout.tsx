import React from 'react'

const AdminLayout = ({
    children,
}: {
    children: React.ReactNode;
}) => {
    return (
        <>
            {children}
        </>
    )
}

export default AdminLayout